{
  "private": true,
  "workspaces": ["packages/*"],
  "packageManager": "npm@11.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "format": "prettier --write .",
    "lint": "eslint --fix ."
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "turbo": "^2.4.0",
    "typescript": "^5.6.3"
  }
}
