{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint --fix ."
  },
  "devDependencies": {
    "@gurgelio/eslint-config": "^0.4.0",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
